# Domain glossary

- **Browser Tab**: 使用者在已開啟的 Chrome 或 Firefox 視窗內選取的單一分頁；它是發送的目標，不等同於 OS 視窗。
- **快捷鍵**: 使用者可建立並命名的單一組合按鍵，例如 `F8` 或 `Ctrl+8`；不包含多步驟序列、文字、滑鼠或延遲。
- **發送**: 對兩個已選 Browser Tab 嘗試套用同一個快捷鍵的使用者動作；各目標的結果必須可辨識。
- **目標選取**: 以 UI 下拉選單選定一次發送的兩個 Browser Tab；它是 session-only 狀態，App 重啟後必須重新選擇。
- **全域冷卻**: 一次被 App 接受的發送後，全 App 在 60 秒內不得接受任何新的發送；其目的是避免重複送出，不以目標或快捷鍵區分。
- **持久化設定**: 跨 App 重啟儲存並還原的使用者設定；不包含目標選取。
- **已連線瀏覽器**: 已安裝對應 WebExtension 並與 native host 建立當次 session 連線的 browser profile；只有它回報的 Browser Tab 可被目標選取。
- **盡力原生發送**: 依序重新驗證、啟用及聚焦目標 Browser Tab 後，嘗試由 native host 注入快捷鍵的流程；它不保證輸入為 trusted、送達或原子完成。
- **發送結果**: 發送的可觀察結果；注入前失敗即整次拒絕，首個目標已嘗試後才發生的失敗則為 partial outcome，禁止自動重試。
- **權限閘門**: 缺少輸入授權或目前桌面工作階段不支援輸入注入時，阻止盡力原生發送並提供修復資訊的條件。
- **發送協調器**: 擁有目標選取、全域冷卻、發送順序與發送結果的桌面 App；它是使用者可見狀態的唯一權威。
- **瀏覽器 adapter**: 由每個 browser-specific WebExtension 提供的控制面，負責 Browser Tab 枚舉、session 驗證與啟用／聚焦，不負責注入快捷鍵。
- **原生輸入 adapter**: 由 allowlisted Native Messaging host 承載的 OS 輸入嘗試層；它只執行發送協調器授權的盡力原生發送。
- **Tab metadata**: 僅於當次 session 在本機 UI 顯示的 browser、window 與 tab title 資訊；不得持久化、外傳或用來重建目標選取。
- **不同目標規則**: 同一發送的兩個目標選取必須指向不同的 Browser Tab。
- **前景閘門**: 在每次輸入嘗試前，若無法驗證預期 browser window 仍是可接收輸入的前景目標，立即停止且不重新搶焦點的安全規則。
- **焦點還原**: 只在前景仍是 App 最後啟用目標時，best-effort 還原發送前前景視窗的行為；任何使用者中途切換優先於還原。
- **發送確認**: 使用者按下顯示目標與快捷鍵的 `發送`按鈕即完成一次發送確認；不使用額外 modal。
- **快捷鍵選擇**: 一次發送所選的唯一快捷鍵；同一筆發送的兩個 Browser Tab 必須共用它。
- **發布證據**: 與特定 release artifact 綁定、不可覆寫的驗證紀錄；按平台、桌面工作階段與兩目標 browser 配對，保存版本、SHA-256、結果、失敗輸出與影音證據連結，作為支援宣稱的依據。
